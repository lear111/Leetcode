class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        n = len(List)
        for a in range(n):
            for b in range(a):
                if List[a] + List[b] == int:
                    list = [a,b]
                    return list